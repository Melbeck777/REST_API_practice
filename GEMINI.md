#### 0. 目的

- 既存APIに対して **hurlのテスト（回帰向け）を網羅的に作成**する。
- 仕様変更時に壊れにくいよう、**テストの前提（seed/fixture）と期待値を明確化**する。
- 実外部APIへの依存がある場合は、**観測（observe）と検証（verify）を分離**する。

#### 1. 成果物

- `hurl/` 配下にhurlファイル一式を追加する。
- `hurl/env/` に環境変数定義（例: `dev.env` `stg.env`）を追加する。
- `hurl/README.md` に実行方法を追記する。
- テストケース一覧（coverage）を `hurl/coverage.md` に作る。

#### 2. ディレクトリ構成（まずはPoC用の最小）

- `hurl/verify/`
  - CIで回す想定（外部に当てない）
  - エンドポイント単体の検証（レスポンスshape、必須項目、ステータス、基本的な境界値）
- `hurl/observe/`
  - 手動実行の調査用（必要なら外部に当ててもよい）
  - 取得したレスポンスをfixtureとして保存する導線を用意
- `hurl/env/`
  - `local.env` `dev.env` `stg.env` など
- `hurl/fixtures/`（必要になったら）
  - 外部APIレスポンスのキャプチャJSONなど

#### 3. 変数化ポリシー

- 変数化する
  - `BASE_URL`（自分のAPI）
  - 認証情報（token等。ファイルに直書きしない）
  - ケースごとの差分値（ID、検索条件、limitなど）
- 原則固定
  - エンドポイントのパス
  - “そのテストの主旨” である期待値（ただし壊れやすいものはshape検証に寄せる）

#### 4. 網羅の定義（各エンドポイントで最低限作るケース）

各エンドポイントについて、可能な限り以下の観点でケースを用意する。

- 正常系
  - 最小入力（必須のみ）
  - 代表入力（典型的な値）
- 入力バリデーション
  - 必須欠落（400系になるか）
  - 型違い / フォーマット違い（例: UUID不正、日付不正）
  - 範囲外（min/max、limit上限）
- 権限/認証
  - トークンなし
  - 権限不足（403など）
- 存在しないリソース
  - 404（存在しないID）
- 冪等性・二重実行（該当する場合）
  - 同一リクエスト2回で整合が取れるか
- ページング/ソート（該当する場合）
  - `limit=1` `limit=最大` `offset` `cursor`
- 出力のshape
  - 必須キーが存在する
  - null許容が意図通り
  - 配列の要素の最低限のフィールド

#### 5. テストの書き方ルール（hurl）

- assertは「値の完全一致」より「shape/条件」を優先する。
  - 例: `status == 200`、`jsonpath "$..id" exists`、`matches` などを中心
- ただし、仕様として固定すべきもの（enum、ステータス、フラグ）は完全一致で縛る。
- 1ファイル=1ケースに寄せる（PoC段階は読みやすさ優先）。

#### 6. 前提データ（seed）の扱い

- 参照系テストが成立するように、テスト用の固定データを準備する。
- もし固定データが用意できない場合は、
  - `observe/` で取得→fixture化
  - `verify/` はfixture/スタブで成立させる

#### 7. 生成AIにやってほしい作業（手順）

1. リポジトリを解析し、APIのエンドポイント一覧を抽出する。
   - OpenAPI（Swagger）がある場合はそれを最優先。
   - なければcontroller/routerから推定。
2. エンドポイントをグルーピングする。
   - read（GET）/ write（POST/PUT/PATCH/DELETE）
   - 認証必須/不要
   - 外部API依存あり/なし
3. `hurl/coverage.md` を先に作る。
   - エンドポイント × ケース（正常/異常/権限/404…）の表
4. `hurl/verify/` にhurlケースを生成する。
   - まず重要度が高い順（フロント表示に直結、障害が出やすい箇所）
5. 実行方法を `hurl/README.md` に書く。
   - `hurl --variables-file ...` の例
   - レポート出力の例

#### 8. 禁止事項

- 本番トークンや個人情報をコミットしない。
- 実外部API呼び出しを `verify/` に入れない。

#### 9. 追加で欲しい情報（AIが不明な場合は質問する）

- テスト環境の `BASE_URL` と認証方式
- テスト用の固定データ（存在するIDなど）
- OpenAPIの有無
